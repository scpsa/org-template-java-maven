# CI metadata — consumed by scpsa/org-cicd auto-ci dispatcher.
# See https://github.com/scpsa/org-cicd/blob/main/docs/ci-metadata.md
language: java
build: maven
working-directory: code
tests: true
security-scan: true
